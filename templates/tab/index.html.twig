{% extends 'template.html.twig' %}

{% block body %}
<table class="table">
    {% for note in notes %}
    <tr>
        <td>{{ note }}</td>
    </tr>
    {% endfor %}
</table>
<div class="alert alert-info">
    <p>Nombre des projets: {{ notes | length }}</p>
    <p> minimale : {{ min(notes) }}</p>
    <p> maximale : {{ max(notes) }}</p>

</div>
{% endblock %}